extends ../template/master.pug

block content
	div(style='float: left;')
		h1.title(style='color: green;') Post Categories
	div(style='float: right;')
		a(href=`/home`)
			button.button(type='button')
				strong(style='color: green;') Back to HOME
	div
		br
		br
		hr
		each post in posts
			td: a(href=`/browse/${post.id}`)
				strong
					li= `${post.id} - ${post.category_name}`
			br
